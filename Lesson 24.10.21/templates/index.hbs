<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> 
        {{#if title}}
        {{{title}}}
        {{/if}}
    </title>
</head>
<body>
    <h1>Hello, {{{name}}} {{{second_name}}}! Твой возраст {{{year}}}, рост {{{height}}}</h1>
    <ul>
        {{#each list}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    <ul>
        {{#each keys}}
        <li>{{@key}}-{{this}}</li>
        {{/each}}
    </ul>
</body>
</html>